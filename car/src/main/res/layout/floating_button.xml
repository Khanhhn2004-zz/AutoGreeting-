<?xml version="1.0" encoding="utf-8" ?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="#88000000"
>

    <!-- Floating Button 1 - Hello Sound -->
    <FrameLayout
        android:id="@+id/floating_card_1"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/floating_button_hello_bg"
        android:elevation="8dp"
    >
        <ImageView
            android:id="@+id/floating_button_1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            android:src="@drawable/ic_notifications"
            android:contentDescription="Play hello sound"
            android:scaleType="fitCenter"
        />
    </FrameLayout>

    <!-- Floating Button 2 - Goodbye Sound -->
    <FrameLayout
        android:id="@+id/floating_card_2"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/floating_button_goodbye_bg"
        android:elevation="8dp"
    >
        <ImageView
            android:id="@+id/floating_button_2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            android:src="@drawable/ic_phone"
            android:contentDescription="Play goodbye sound"
            android:scaleType="fitCenter"
        />
    </FrameLayout>

</LinearLayout>
